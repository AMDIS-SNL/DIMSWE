[pytest]
markers =
    characterization: non-gating observations of unresolved current behavior
    jax: tests requiring the optional Firedrake JAX extra
    rol: tests requiring the optional rol-python distribution
