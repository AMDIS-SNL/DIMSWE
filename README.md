# hamiltonian-model


## Requirements

This project requires Firedrake.

The recommended way to run tests and examples is via the Firedrake Docker image:

    firedrakeproject/firedrake

or on an HPC system with a working Firedrake installation.
