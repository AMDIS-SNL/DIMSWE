# Track 1 provenance and disposition

## Chain of custody

Authoritative archaeological checkout (read-only):

`/Users/arjunsharma/Documents/SandiaProjects/4-LDRDAMDIS/DIMSWE-study-d0eb615`

Collaborator-facing filesystem snapshot:

`/Users/arjunsharma/Documents/SandiaProjects/4-LDRDAMDIS/DIMSWE-collaborator-track1-20260828`

The source was on branch `dev/dimswe-learned-physics-framework` at
`d2f5d66ecb5500aad24eca37280f8a52e22a250f`. The frozen status contained 10
modified tracked files and 6,938 untracked files (6,948 evidence files total),
including approximately 6.52 GiB under `external-results/`.

The complete freeze inventory is
[`docs/provenance/FROZEN_DIRTY_STATE_MANIFEST.tsv`](docs/provenance/FROZEN_DIRTY_STATE_MANIFEST.tsv),
SHA-256
`010801095f7f4f93dbd54f6c3a70aa742acdbfde2c1fcb36b33923f8a0059a36`.
It records status, byte count, SHA-256, MIME type, and original path for every
evidence file. A post-isolation comparison reported all 6,948 files `MATCH`; the
verification table has SHA-256
`75281692acf1d78927ce6ead7b3e4bc3a0906196d6e284c4dc8da7f885936649`.

## Linked-worktree issue and resolution

The first filesystem copy inherited a 107-byte `.git` *pointer file*, not
independent repository metadata. Its exact content is preserved in
[`docs/provenance/INHERITED_LINKED_WORKTREE_POINTER.txt`](docs/provenance/INHERITED_LINKED_WORKTREE_POINTER.txt),
SHA-256
`63137c597f563232672c667b32f4ae973c3adfd83f31abe00764f7e7d238b00b`.
It pointed at the authoritative common repository's
`.git/worktrees/DIMSWE-study-d0eb615` entry.

Before any collaborator commit, a temporary `git clone --no-checkout
--no-hardlinks` was made from the complete local history. Its independent `.git`
directory replaced only the collaborator copy's pointer. The index was populated
with `git read-tree d2f5d66...`; no checkout/reset was applied to the evidence
tree. Checks then established:

- both `git rev-parse --git-dir` and `--git-common-dir` resolve inside the
  collaborator directory;
- `HEAD` is exactly `d2f5d66...` and baseline `d0eb615...` is present and
  ancestral;
- there are no alternates, symlinked Git metadata, or shared hard-linked object
  files;
- all 6,948 frozen evidence hashes still match; and
- the authoritative branch, `HEAD`, status hashes, pointer hash, and remotes did
  not change.

The collaborator fetch provenance remains the GitHub URL. Push is set to
`DISABLE_PUSH`; no local authoritative repository is a push target.

## File dispositions

[`docs/provenance/DISPOSITION_MANIFEST.tsv`](docs/provenance/DISPOSITION_MANIFEST.tsv)
maps every frozen evidence path to its classification, destination, disposition,
reason, size, and source hash. It is generated by
`archive/track1-forensic-tools/build_track1_disposition_manifest.py` from the
immutable freeze table. The generator moved out of the normal reproduction
surface during the second-pass hygiene audit; the generated table itself is
the frozen Track 1 record.

The initial classification totals are:

| Classification | Files |
|---|---:|
| CANONICAL_SOURCE | 57 |
| CANONICAL_TEST | 29 |
| CANONICAL_CONFIG | 22 |
| CANONICAL_DOC | 42 |
| CANONICAL_PARAMETER_ARTIFACT | 668 |
| CANONICAL_RESULT | 449 |
| GENERATED_REPRODUCIBLE_RESULT | 1,294 |
| ARCHAEOLOGY | 4,240 |
| SUPERSEDED | 17 |
| OBSOLETE | 76 |
| UNKNOWN | 54 |

The 668 parameter-class entries include intermediate/checkpoint artifacts, not
668 independently canonical final models. Their per-file dispositions distinguish
compact final artifacts from local-only evidence.

The generated table assigns 331 files `RETAINED_VERSIONED`, 6,541
`RETAINED_LOCAL_NOT_VERSIONED`, and 76 `EXCLUDED_GENERATED`. The 181 selected
external-result paths are listed separately in
`docs/provenance/VERSIONED_CANONICAL_ARTIFACTS.txt`; that list has SHA-256
`4c083a577ac379e1f9161ee7cfa7e5af16737a8ae2398e9af9ea9278155f80f7`.
The full disposition table has SHA-256
`48cbcd7b9f31369bb142a52ecef73b18620870c3c4c9012d727058ccfd65866e`.

Only the 76 recorded `.DS_Store`/`__pycache__`/bytecode files were removed from
the collaborator copy. They remain recoverable from the authoritative checkout
and are fully hashed in the freeze manifest. No scientific file was deleted or
moved. Uncertain and superseded material remains in place, locally ignored by
Git, rather than being silently discarded.

## Distribution boundary

Ordinary Git tracks the implementation, tests, configs, reports, manifests,
compact final parameter files, and compact canonical result JSON. Large HDF5,
NumPy caches, VTK archives, truth trajectories, and raw/preparatory runs remain
in the local snapshot under their original paths. Their hashes are the transfer
contract for a future artifact store, release archive, or Git-LFS migration.

BTP/BTPL M1-only outputs are classified `UNKNOWN`: they are preserved but are
not promoted to a completed B+ campaign. Files explicitly under a
`superseded` path remain `SUPERSEDED`; preparatory and early campaigns remain
`ARCHAEOLOGY`.
